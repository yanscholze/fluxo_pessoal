node_modules/
.expo/
.eas/
.home/
.npm-cache/
dist/
*.log

# `android/` é gerado por `expo prebuild` a partir de `app.config.js` e dos
# plugins — inclusive a injeção do listener de notificações no manifesto e no
# MainApplication. Versionar o gerado convida a editar ali e perder a edição no
# próximo prebuild.
android/
